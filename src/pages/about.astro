---
import { Schema } from 'astro-seo-schema'
import siteConfig from '@/site.config'
import * as entry from '@/content/about.mdx'
import Layout from '@/layouts/Layout.astro'
import { ArticleContent, ArticleMediumZoom, ArticleTitle } from '@/components/article'
import { Footer } from '@/components/partials'
import '@/styles/index.css'

const { Content } = entry
---

<Layout title={entry.frontmatter.title}>
  <Fragment slot="head">
    <Schema
      item={{
        '@context': 'https://schema.org',
        '@type': 'BlogPosting',
        headline: entry.frontmatter.title,
        description: entry.frontmatter.description,
        image: [],
        author: {
          '@type': 'Person',
          name: siteConfig.author,
          url: Astro.site?.href,
        },
      }}
    />
    </script>
  </Fragment>

  <div class="container py-8 md:!max-w-3xl xl:py-12">
    <article>
      <ArticleTitle>{entry.frontmatter.title}</ArticleTitle>

      <div class="mt-8 md:mt-10 mb-8">
        <ArticleContent>
          <Content />
        </ArticleContent>
      </div>
    </article>
  </div>

  <div class="container">
    <Footer />
  </div>

  <Fragment slot="body">
    <ArticleMediumZoom />
  </Fragment>
</Layout>
